extends layout.pug

block content
  article.grid-container.portfolio-intro
    div

      h1  Hi, how are you ? I'm Joe!

      p.lead.text-light
        | I am a London-based Junior JavaScript Developer. I love JavaScript and everything that comes with it :) <br />
        | This is my Portfolio!
  article.grid-container.portfolio-index

    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      //- pug `each` iterator to create a project img, title and link in the markup for each of the projects you are showcasing.

      each project in projects
       .cell
        a(href=`/project-${project.id}`)
          img.thumbnail( src=`${project.image_urls[0]}` )
          h5= project.project_name
